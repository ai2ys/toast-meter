## Commit conventions

- Use Conventional Commits.
- Format: `type(scope): summary` when a scope is helpful, otherwise `type: summary`.
- Prefer: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`.
- Keep feature commits and release/version commits separate.
- Use release commits like: `chore(release): 0.2.0`.
